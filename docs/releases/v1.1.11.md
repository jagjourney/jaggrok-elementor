# AiMentor Elementor v1.1.11 Draft Release Notes

## Summary
- Finalize the cooldown messaging rollout by bumping plugin metadata and distribution collateral to `v1.1.11`.
- Keep Elementor users informed about provider throttling with localized retry guidance in the widget modal.
- Ensure the release manifest and landing copy reference the new GitHub asset so automatic updates pull the latest ZIP.

## Testing
- php -l aimentor-elementor.php
- php -l includes/providers/class-aimentor-grok-provider.php
- php -l includes/providers/class-aimentor-openai-provider.php

## Release Checklist
- [ ] Tag the commit as `v1.1.11` once merged to `main`.
- [ ] Draft the GitHub release using the summary above and attach the generated `aimentor-elementor-v1.1.11.zip` asset (built by CI).
- [ ] Publish the release to trigger `.github/workflows/release.yml` and verify the manifest update on `gh-pages`.
