# AiMentor Elementor v1.1.00 Draft Release Notes

## Summary
- Surface provider cooldown guidance in the Elementor widget so editors know when they can retry after hitting rate limits.
- Parse xAI Grok and OpenAI rate-limit headers, normalizing the metadata into AJAX responses for both success and error payloads.
- Localize reusable cooldown messaging strings and ensure documentation, manifests, and plugin metadata reflect `v1.1.00`.

## Testing
- php -l aimentor-elementor.php
- php -l includes/providers/class-aimentor-grok-provider.php
- php -l includes/providers/class-aimentor-openai-provider.php

## Release Checklist
- [ ] Tag the commit as `v1.1.00` once merged to `main`.
- [ ] Draft the GitHub release using the summary above and attach the generated `aimentor-elementor-v1.1.00.zip` asset (built by CI).
- [ ] Publish the release to trigger `.github/workflows/release.yml` and verify the manifest update on `gh-pages`.
