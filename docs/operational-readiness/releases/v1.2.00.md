# AiMentor Elementor v1.2.00 Draft Release Notes

## Summary
- Persist successful canvas payloads in a reusable history so editors can revive layouts without re-running the API.
- Surface recent layouts inside the Elementor widget placeholder with a carousel that previews summaries and inserts sections instantly.
- Refresh plugin metadata, manifest references, and landing collateral to publish the 1.2.00 feature release.

## Testing
- php -l includes/settings.php
- php -l aimentor-elementor.php
- npm run lint (or npx eslint js/elementor-widget.js)

## Release Checklist
- [ ] Tag the commit as `v1.2.00` once merged to `main`.
- [ ] Draft the GitHub release using the summary above and attach the generated `aimentor-elementor.zip` asset (built by CI).
- [ ] Publish the release to trigger `.github/workflows/release.yml` and verify the manifest update on `gh-pages`.
